<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Skill Sets Shown in Express</title>
</head>
<body>
    <a href="/">Home</a>
    <h1>All Skills</h1>
    <ul>
    <% skills.forEach(function(s) { %>
        <li>
            <a href="/skills/<%= s.id %>"><%= s.skill %></a>
             - 
            <%= s.have ? 'have' : 'dont have' %>
        </li>
    <% }); %>
</ul> 
</body>
</html>